version: '3.6'
services:
  postgres:
    image: postgres/postgres:11
    ports:
      - "5432:45432"
    restart: always
    environment:
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: password
      POSTGRES_DB: knexapp
